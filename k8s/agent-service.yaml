apiVersion: v1
kind: Service
metadata:
  name: agent-flow-agent
  labels:
    app: agent-flow-agent
spec:
  type: ClusterIP
  selector:
    app: agent-flow-agent
  ports:
    - port: 8000
      targetPort: 8000
      protocol: TCP
      name: http
